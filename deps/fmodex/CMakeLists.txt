if(WIN32)
    set(FMODEX_DIR "${CMAKE_CURRENT_LIST_DIR}/win" PARENT_SCOPE)
else()
    set(FMODEX_DIR "${CMAKE_CURRENT_LIST_DIR}/linux")
endif()

set(FMODEX_INCLUDE_DIR "${FMODEX_DIR/include}" PARENT_SCOPE)
set(FMODEX_LIB_DIR "${FMODEX_DIR/lib}" PARENT_SCOPE)